# Agency SEO

1. One workspace per client. Client-a cannot read client-b.
2. Secrets never enter `config.json`.
3. Start `--mode audit`, then `--mode strategy`. Share reports (executive + technical) before implementation.
4. Growth mode is for retainers: alerts are signals, not diagnoses.
