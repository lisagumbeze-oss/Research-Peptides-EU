# SaaS SEO

1. Classify rendering (SSR/SSG vs CSR app shell).
2. `sge orchestrate --mode audit` — indexable marketing surfaces, not the logged-in app.
3. Map pricing, comparison, integration and docs clusters from observed GSC/GA4 only.
4. `sge orchestrate --mode strategy` — topic map + roadmap. Do not create pages for every keyword permutation.
5. Implementation stays dry-run until `--apply`.
