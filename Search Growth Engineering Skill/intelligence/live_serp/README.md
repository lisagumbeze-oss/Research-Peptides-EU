Live SERP snapshots live here (Python package `intelligence.live_serp`).

Collect only through an authorized SERP provider. Never scrape Google or Bing HTML.
Raw provider payloads are stored under `raw/` separately from derived snapshots.
