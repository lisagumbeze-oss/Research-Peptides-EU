# Examples

Workspace template: `workspaces/example/`.

Playbooks: `playbooks/`.

Do not commit client metrics. Copy `config/platform.example.json` into a workspace `config.json` and keep secrets in `.env` or a secret manager.
